# super-termux

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)

终端复用器（Terminal Multiplexer）—— 模块化 C 架构，单文件可执行。
在一个终端窗口里管理多个 shell 会话，像 tmux 一样分标签页、分屏、搜历史。

当前版本：**v2.1.8**（正式支持 Windows / Linux / macOS 三个系统）

## 平台支持

| 系统 | 后端 | 产物 | 状态 |
|---|---|---|---|
| Windows 10 1809+ | ConPTY + `CreateProcessW` | `termux.exe` | ✅ CI 每次构建 |
| Linux (glibc) | `forkpty` + termios raw | `termux-linux` | ✅ CI 每次构建 |
| macOS (Apple silicon / Intel) | `forkpty`（libSystem） | `termux-macos` | ✅ CI 每次构建 |

三个系统共用同一份引擎代码（screen / render / input / split / vt / theme / config …），
只有平台相关的部分抽在平台层：

```
Windows : src/main.c + src/platform_win.c + src/conpty_loader.c
POSIX   : src/main_posix.c + src/platform_posix.c + src/term_input_posix.c
接口    : include/platform.h
```

后端映射：ConPTY ↔ `forkpty`；`ResizePseudoConsole` ↔ `TIOCSWINSZ`；
`WriteConsoleA` ↔ `write(1)`；`ReadConsoleInputW` ↔ termios raw 自解析；
`GetConsoleScreenBufferInfo` ↔ `TIOCGWINSZ`；`WINDOW_BUFFER_SIZE_EVENT` ← `SIGWINCH`；
剪贴板 ↔ OSC 52 + `pbcopy` / `wl-copy` / `xclip`。

> ⚠️ **警告 / 注意事项**：
> 控制台终端**必须配置使用等宽字体**（Monospace Font，例如 *Cascadia Code*、*Consolas*、*JetBrains Mono*、*Fira Code* 等）。
> 使用非等宽字体会导致字符宽度计算偏差、边框排版错位及界面渲染故障。

## 特性

- **多标签页**：每个标签一个独立的 cmd / PowerShell 会话（ConPTY 后端，需 Win10 1809+）
- **GitHub-Dark 风格 UI**：真彩色（24bit）渲染，中文界面
- **标签页染色**：8 色调色板，右键标签 → 改颜色 / 改标题，或 `Ctrl+B t` 轮换
- **鼠标支持**：点击/中键/右键标签操作、滚轮滚动、hover 高亮
- **滚动历史**：10000 行环形滚动缓冲，PgUp/PgDn/滚轮查看
- **Alt 屏幕支持**：nano / vim 等全屏编辑器正常使用，退出后历史完整保留
- **内置帮助**：点击左上角 `termux` 徽标查看
- **分层命令面板**：仅通过 `Ctrl+B :` 打开（同时兼容中文全角冒号 `：`）；普通终端直接进入“操作命令面板”，设置页直接进入“设置命令面板”。每次最多显示 9 个结果，当前窗口始终重新编号为 `1`～`9`（序号用普通文字色，不上色块）；滚轮直接滚动结果列表，选中项跟着页面等量平移、在窗口中的相对位置不变；Tab 在搜索输入框与结果选择框之间切换，输入焦点下数字会继续搜索，结果焦点下数字选择当前可见项。操作面板支持新建终端（子面板内可按名称/命令搜索）、自定义命令行、标题/颜色、历史搜索、panel 切换（按编号或标题）、复制模式、分屏（左右/上下切分、切换窗格、全屏缩放、关闭窗格）、热重载、打开图形化设置、独立 About panel、关闭当前 panel、退出整个 termux，并可切换到设置命令面板；设置面板包含打开操作命令面板、默认启动项、打开 `.ini`、添加 panel 条目（预设/自定义后继续编辑）与菜单项设置。菜单项设置子面板只管理已有条目：Enter 编辑、`Ctrl+↑/↓` 调整位置（搜索期间禁用）、无查询时结果焦点下 `X` 删除；搜索输入焦点下 `U/D` 仍是普通查询字符，有查询时使用 `Ctrl+X` 删除；添加 panel 仍从设置面板的独立入口执行。Esc 返回子面板时会恢复父面板的选择、滚动位置和查询上下文。
- **可配置的键位**：前缀键（默认 `Ctrl+B`）与每个动作的按键都能在 `termux.ini` 的 `[keys]` 段里改，每个动作还能单独设为「不带前缀的直接键」（`noprefix`，设置页键位表里按 `P` 或点 `[前缀]/[直接]` 列切换）；帮助页显示的快捷键跟着配置实时变化
- **右上角状态徽章**：搜索输入框与复制/搜索状态全部收在右上角，不再占用任何一整行终端内容；复制模式与历史搜索都折叠成右上角一枚小徽章（`[复制模式 行选区]` / `[搜索 "err" 3/12]`），鼠标悬停时才向左展开完整操作提示，不再长期占据一整行
- **配色主题**：内置 `github-dark` / `one-dark` / `nord` / `gruvbox-dark` / `dracula`，也可在 `[theme]` 段按语义角色单独覆盖任意一种颜色；`Ctrl+B` → 设置命令面板 → 切换配色主题 可即时轮换
- **行为开关**：`scrollback` 滚动行数、`mouse` 鼠标开关、`copy_on_select` 拖选自动复制、`confirm_on_exit` 退出二次确认、`confirm_on_close` 关闭窗格/标签二次确认
- **每个菜单项可配启动默认颜色**：菜单项详情页（以及命令面板的 panel 编辑器）多了一条颜色选择条，`←/→`、数字 `0-8` 或鼠标点选即可指定这一项新建标签页时的颜色，写回 `termux.ini` 的 `color=` 字段
- **图形化设置页**：`Ctrl+B s` 打开，侧栏含「启动 / 条目管理 / 外观·主题 / 键位 / 行为 / 窗格配色」六类；「启动」页只用来选**默认启动项**（表是只读的，`Enter/E/M/数字键` 带去条目管理页，双击直接进该条详细配置），增删改与排序（`[+] 新建条目`、`[P] 预设库`、每行 `[↑][↓][改][删]`、`Ctrl+↑/↓` 调序、`X` 删除）都在**「条目管理」子页**里；主题可即时预览切换、语义色可改十六进制、键位支持**按键录制**，改完即时写回 `termux.ini`。窗口再小也不缺内容：矮终端下每页可上下滚动（**滚轮**或 `↑/↓`，提示行标出行窗口），窄终端下表格按虚拟画布排版、**Shift+滚轮横向滚动**看被挤掉的名称/命令行（聚焦行会自动滚进视口），其余装不下的文字截断为 `...` 并**鼠标悬停浮出完整内容**；改颜色时弹出**居中的十六进制编辑浮层**（色块 + 完整 6 位），任何宽度都不被裁
- **诊断日志**：`TERMUX_DUMP=1` 时输出原始 ConPTY 流 / 渲染输出 / 鼠标事件

## 快捷键

| 快捷键 | 功能 |
|---|---|
| `Ctrl+B :` | 打开命令面板 (Command Palette) |
| `Ctrl+B c` | 新建默认 pane |
| `Ctrl+B +` | 新建 pane 菜单（选已配置项目 / 自定义命令行） |
| `Ctrl+B [` | 进入复制模式（方向键/hjkl 移动、Space/v 选区、`Shift+方向` 行选、`Alt+方向` 块选、`b` 切换行/块、Enter/`Ctrl+C`/y 复制、Esc 退出） |
| `Shift+点击两点` | 直接进入复制模式并选中两点之间（行选）；`Alt+点击两点` 为矩形框选。`Ctrl+C`/Enter 复制并关闭，Esc 退出，其它键退出并把该键发给终端 |
| `Ctrl+B /` | 搜索滚动历史（`U`/`D` 或 `n`/`N` 跳转上一个/下一个匹配，Esc 退出；右上角徽章上的 `[U 上] [D 下] [×]` 也可鼠标点） |
| `Ctrl+B s` | 打开图形化设置页面 (`termux.ini`) |
| `Ctrl+B r` | 热重载配置文件 (`termux.ini`) |
| `Ctrl+B ?` / `h` | 打开 / 关闭帮助 |
| `Ctrl+B n / p` | 下一个 / 上一个 pane |
| `Ctrl+B x` | 关闭当前 panel |
| `Ctrl+B d` | 退出 termux |
| `Ctrl+B t` / `Shift+t` | 轮换标签颜色 |
| `Ctrl+B w` | 打开「切换 panel」可视化面板（按编号或标题选择切换 pane；数字直跳已移除） |
| `Ctrl+B -` / `_` | 分屏：左右切分 / 上下切分（同一标签内多个终端同屏） |
| `Ctrl+B Tab` / `Shift+Tab` | 分屏：切换到下一个 / 上一个窗格 |
| `Ctrl+B ↑/↓/←/→` | 分屏：切换到对应方向的相邻窗格 |
| `Ctrl+B Shift+方向` | 分屏：拖动分隔线调整窗格大小 |
| `Ctrl+B q` | 分屏：关闭当前窗格（同标签只剩一个窗格时关闭整个标签页；可在设置里开启关闭二次确认） |
| `Ctrl+B z` | 分屏：当前窗格全屏缩放 / 还原 |

> 上表是默认键位；前缀键与每个动作的按键都可以在 `termux.ini` 的 `[keys]` 段里改，见下文。

## 鼠标操作

| 操作 | 功能 |
|---|---|
| 左键点击标签 | 切换 pane |
| 左键点击 `×` | 关闭该 pane |
| 右键点击标签 | 改颜色 / 改标题 |
| 中键点击标签 | 关闭该 pane |
| 鼠标左键拖拽 | 框选终端文字，松开自动复制到剪贴板 |
| 点击 `[+]` | 新建 pane（选已配置项目 / 自定义命令行） |
| 点击 `[*]` | 打开图形化设置页面 |
| 点击 `termux` | 打开 / 关闭帮助 |
| 滚轮 | 滚动历史（未开鼠标追踪时） |

## 编译

**Windows**（需要 MinGW-w64；MSVC 也可）

```bat
:: MSVC
cl /O2 /Iinclude src\*.c /Fe:termux.exe /link user32.lib shell32.lib

:: MinGW-w64 (GCC / Make)
make
:: 或手动执行
x86_64-w64-mingw32-gcc -O2 -s -Wall -Wextra -Iinclude src/*.c -o termux.exe -luser32 -lshell32
```

**Linux**

```sh
make linux          # -> ./termux-linux（需要 -lutil 提供 forkpty）
```

**macOS**（必须在 macOS 上编：`forkpty` 在 libSystem、没有 `-lutil`；
`_NSGetExecutablePath` 在 `<mach-o/dyld.h>`。Makefile 里有 `uname -s` 护栏，
在别的系统上跑 `make darwin` 会直接 exit 1，不会产出一个改名的 Linux 二进制冒充）

```sh
make darwin         # -> ./termux-macos
```

**跑测试**

```sh
make check-posix    # POSIX 侧一把梭：编译 + 79+ 条移植不变量 + 4 个单测 + 真 pty 冒烟
make unittest       # 主题 / 键位 / 配置（-Werror）
python3 verify_all.py
```

三个系统的完整检查见 [`.github/workflows/ci.yml`](.github/workflows/ci.yml)。

## 运行

```bat
termux.exe          :: Windows
set TERMUX_DUMP=1   :: 启用诊断日志（termux_dump.log / render_dump.log / mouse_dump.log）
```

```sh
./termux-linux      # Linux
./termux-macos      # macOS
TERMUX_DUMP=1 ./termux-linux
```

## ⚙️ 配置文件 (termux.ini)

- **配置文件路径**：优先读取 `termux.exe` 所在目录下的 `termux.ini`；若不存在则读取 `%USERPROFILE%\.termux.ini`。首次启动会自动生成默认配置文件。
- **热重载**：`Ctrl+B r`（或命令面板的“热重载”）即时生效。`scrollback` 只对之后新建的 pane 生效，其余项立即生效。
- 配置文件分四段：`[general]` 行为、`[theme]` 配色、`[keys]` 键位、`[menu]` 新建菜单。

```ini
# super-termux 配置文件 (UTF-8)

[general]
theme = github-dark        # github-dark | one-dark | nord | gruvbox-dark | dracula
prefix = C-b               # 前缀键：C- = Ctrl，M- = Alt，S- = Shift
scrollback = 10000         # 每个 pane 的滚动历史行数 (200 - 500000)
mouse = true               # 关掉后标签点击 / 拖选 / 滚轮全部停用
copy_on_select = true      # 鼠标拖选松开时自动复制到剪贴板
confirm_on_exit = false    # 退出 termux 前弹出 Y/N 二次确认
confirm_on_close = false   # 关闭窗格 / 标签前弹出 Y/N 二次确认
search_case_sensitive = false  # 历史搜索是否锁定大小写（false = 忽略大小写）
anim = normal              # 过渡动画（切页 / 浮层 / 气泡 / 切标签横滑）：off | short(60ms) | normal(110ms)，也可直接写毫秒数(≤600)
default_startup = 0        # 0 = 启动进终端，1 = 启动显示帮助

[theme]
# 按语义角色覆盖任意颜色，只写想改的那几行即可
# accent = #58a6ff
# background = #0d1117

[keys]
# 动作名 = 键位[ noprefix]
# 默认要先按前缀键；加上 noprefix（或 direct）就是不带前缀的直接键
# new-pane = c
# next-theme = T
# copy-mode = F8 noprefix

[menu]
# 序号 = 菜单显示名称, 启动命令行, 启动目录(可选), color=启动默认颜色(可选 1-8)
# 特殊命令 ":custom" 表示打开自定义命令行输入框
# color 省略或写 0 表示跟随默认蓝色
1 = cmd, cmd.exe
2 = PowerShell, powershell.exe, , color=2
3 = 自定义命令行, :custom
# 4 = WSL Ubuntu, wsl.exe -d Ubuntu
# 5 = Git Bash, "C:\Program Files\Git\bin\bash.exe" --login -i
# 6 = 项目终端, cmd.exe, D:\work\myproject
```

### `[keys]` 可绑定的动作

| 动作名 | 说明 | 默认键位 |
|---|---|---|
| `send-prefix` | 把前缀键本身发给当前 pane | 连按两次前缀键 |
| `command-palette` | 打开命令面板 | `:` |
| `new-pane` | 新建默认 pane | `c` |
| `new-pane-menu` | 新建 pane 菜单 | `+` |
| `copy-mode` | 进入复制模式 | `[` |
| `search` | 搜索滚动历史 | `/` |
| `settings` | 打开图形化设置 | `s` |
| `reload-config` | 热重载配置 | `r` |
| `help` | 打开 / 关闭帮助 | `?` / `h` |
| `next-pane` / `prev-pane` | 下一个 / 上一个 pane | `n` / `p` |
| `close-pane` | 关闭当前 pane | `x` |
| `quit` | 退出 termux | `d` |
| `tab-color-next` / `tab-color-prev` | 轮换标签颜色 | `t` / `Shift+t` |
| `switch-panel` | 切换 panel（打开可视化面板，按编号或标题选择） | `w` |
| `next-theme` | 切换下一个配色主题 | 未绑定 |
| `split-horizontal` | 分屏：上下切分 | `_`（Shift+-） |
| `split-vertical` | 分屏：左右切分 | `-` / `|` |
| `split-next` / `split-prev` | 分屏：下一个 / 上一个窗格 | `Tab` / `Shift+Tab` |
| `split-up` / `split-down` / `split-left` / `split-right` | 分屏：按方向切换窗格 | `↑` / `↓` / `←` / `→` |
| `split-resize-up` / `split-resize-down` / `split-resize-left` / `split-resize-right` | 分屏：拖分隔线调整大小 | `Shift+↑/↓/←/→` |
| `split-close` | 分屏：关闭当前窗格 | `q` |
| `split-zoom` | 分屏：当前窗格全屏缩放 / 还原 | `z` |

键位写法：`C-` = Ctrl，`M-`（或 `A-`）= Alt，`S-` = Shift；键名支持单个字符、`F1`-`F24`、
`Space` / `Tab` / `Enter` / `Esc` / `Backspace` / `Up` / `Down` / `Left` / `Right` /
`Home` / `End` / `PgUp` / `PgDn` / `Ins` / `Del`。例如 `prefix = C-a`、`new-pane = F2`、
`close-pane = M-w`。给某个动作写了绑定，它的默认键位就会被顶掉。

### 图形化设置页（`Ctrl+B s`）

不想手写 ini 的话，所有这些都能在设置页里点：

| 侧栏分类 | 能做什么 | 快捷键 |
|---|---|---|
| 启动 (Startup) | 只管「启动时默认打开哪一个」：两个单选项（默认终端 / 内置帮助）＋一行「当前默认启动项」。**条目表已经搬走**（这一页只有 8 行，任何终端高度都不会溢出），要改条目去 `[M]` | `T` 终端、`H` 帮助、`M` 条目管理 |
| **[M] 条目管理** | 条目的**全部**增删改都在这里：标题行写着「当前默认：xxx」，动作条 `[+] 新建条目` / `[P] 预设库` / `[设为默认]`，每行 `[↑] [↓] [改] [删]`（`[改]` 与 `Enter` 进该条详细配置，`X` 删除），`Ctrl+↑/↓` 调序，`S` 把聚焦那条设为启动默认 | 启动页按 `M` / `S`，或侧栏点这一项 |
| 菜单项详细配置 | 单个条目的名称 / 命令行 / 启动目录 / 启动默认颜色。**侧栏不再列 `[1]`-`[9]`**（用户：左侧不要再额外放这些），从条目管理页点行或按 `[改]`/`Enter` 进来 | `Tab` 切换输入框，`Enter` 保存 |
| **外观 / 主题** | 上下选择内置主题，`Enter`/点击**立即应用并写盘**；下方 16 个语义色带色块与十六进制值，`Enter` 进入编辑（6 位 hex），`R` 复位当前项，`Ctrl+R` 清除全部自定义 | 启动页按 `F2` |
| **键位设置** | 第一行是前缀键，下面是全部 17 个动作；`Enter` 或点击 `[改]` 进入**按键录制**（直接按你想要的组合键即可），`R` 或 `[复位]` 恢复默认，`Ctrl+R` 全部复位 | 启动页按 `F3` / `K` |
| **行为开关** | `mouse` / `copy_on_select` / `confirm_on_exit` / `confirm_on_close` 四个开关，`scrollback` 用 `←/→` 或 `[-] [+]` 按 1000 步进调整 | 启动页按 `F4` / `B` |
| **窗格配色** | cmd / shell 文字的**默认背景、默认前景**、**滚动条滑块 / 轨道**与黑/红/绿/黄/蓝/紫/青/白及 8 个亮色共 20 项（即 `[theme] pane_*`），两列排布，`↑/↓` 选、`←/→` 换列，`Enter` 输 6 位 hex（预填值一打字就整段替换），`R` 复位当前项回「跟随终端」，`Ctrl+R` 全部清除；改完立即生效并写盘。顶部方案行 `Enter` 弹出**配色方案选择列表**（8 套，每行带实际色块预览，`↑/↓`/数字键选、`Enter` 应用、`Esc` 只关列表）。命令面板里也有 `pane-palette` 入口 | 启动页按 `F5` / `W` |

侧栏用鼠标点，或在任意分类页按 `Ctrl+↑ / Ctrl+↓` 依次切换；`Esc` 从分类页返回启动页。
窗口很窄时右侧表格装不下所有列：**Shift + 滚轮 = 横向滚动**（普通滚轮照旧翻行），表头右端那
9 列是**可拖的横条** `«█───»`（也可以直接拿鼠标拖它），聚焦的那一行会自动滚进视野；按钮
`[↑][↓][改][删]` 钉在视口右端，窄到放不下时按「丢命令行 → 收窄名称列 → 丢 `[↑][↓]` → 丢
`[改][删]`」的顺序让位（丢到底仍可用键盘操作）。
窗口太矮、当前页放不下时，**终端最右一列变成该页的纵向滚动条**（滑块 `█` + 轨道 `│`）：拖滑块、
点轨道按页翻、滚轮照旧；只在「这一页真的溢出」时才出现，所以 80/100/120 列 × 正常高度的基准排版
一格未动。矮到 9 行以下时左侧栏那一排入口自己也摆不下，同样给一根细条子（画在分隔线那一列）。
自定义过的语义色行尾会带 `*`，自定义过的键位 `[复位]` 按钮会变红。
切页与浮层不是硬切：整页 110ms 淡入、浮层自上而下逐行点亮；**切换标签页整页左右滑入**，悬停气泡与底部提示条也跟着淡入（`[general] anim = off` 可关，关掉就是逐字节相同的原样输出）。

### `[theme]` 的 16 个语义角色

| 角色名 | 用途 | 角色名 | 用途 |
|---|---|---|---|
| `background` | 最深背景 / 亮底上的文字 | `accent` | 主强调色（活动标签、边框） |
| `tabbar` | 标签栏背景 | `cyan` | 信息色 / 浅蓝 |
| `panel` | 面板与弹窗背景 | `green` / `green_dark` | 成功、绿色标签 |
| `foreground` | 主文字 | `red` | 危险、关闭按钮 |
| `foreground_dim` | 次要文字 | `orange` / `yellow` | 提示、当前匹配 |
| `white` | 高亮文字 | `purple` / `pink` | 品牌色与彩色标签 |
| `selection` | 选区底色 | | |

界面里所有派生色（各种暗色标签底、hover 底色）都由这 16 个角色按固定比例混合得出，
所以只改 `accent` 一行，活动标签、边框、选中行会一起跟着变。

## 系统要求

- **Windows**：Windows 10 1809 (RS5) 或更高（ConPTY 支持）
- **Linux**：任何带 `forkpty`（libutil / glibc）的发行版
- **macOS**：Apple silicon 或 Intel 均可
- 从真实终端窗口运行（Windows Terminal / cmd / ConEmu / iTerm2 / kitty / GNOME Terminal …）
- **必须使用等宽字体**（如 Cascadia Code, Consolas, JetBrains Mono, Menlo 等），
  否则会出现渲染故障与排版错位

## 开发说明

源码采用模块化架构（`include/` 与 `src/`），无第三方依赖，仅链接 `user32`。
每次改动可一键运行仓库内的全部 Python 回归验证：

```bash
python3 verify_all.py
```

主题与键位模块不依赖任何 Win32 调用，可以直接在本机（含 Linux CI）编译执行真正的 C 单元测试：

```bash
make unittest
# 等价于
gcc -Wall -Wextra -Werror -Itests/stub -Iinclude src/theme.c src/keymap.c tests/test_config.c -o test_config && ./test_config
```

也可以单独运行验证脚本：

```bash
python3 verify_picker.py        # 选色器几何 / 点击 / hover 命中
python3 verify_flow.py          # 弹窗端到端流程
python3 verify_mouse53.py       # 鼠标按钮优先级 / 兜底 / 渲染用色
python3 verify_color8.py        # color=8 渲染用色回归
python3 verify_emoji.py         # Emoji 与字素簇边界测试
python3 verify_ringbuf_asan.py  # 环形缓冲区局部滚动 ASAN 内存安全
python3 verify_screen_state.py  # alt 屏 resize 保留真彩色 / 搜索当前项落点
python3 verify_html_clipboard.py # 复制保留颜色：HTML Format 偏移量 / 真彩色 / Campbell 16 色 / RLE
python3 verify_dirty_render.py   # 脏区渲染：整帧切行比对，未变行 0 输出
python3 verify_search.py          # 滚动历史搜索行为验证
python3 verify_item_color.py       # 菜单项启动默认颜色选择条的渲染/热区一致性
python3 verify_search_box.py       # 搜索输入框只画右上角一小框、不吃整行
python3 verify_palette_search.py   # 新建终端短查询/名称优先搜索回归
python3 verify_input_layout.py     # 输入框背景/末尾字符/光标列回归
python3 verify_cursor_render.py    # 终端输出区域最后一格光标回归
python3 verify_menu_settings.py    # 菜单项设置排序/搜索禁用/删除/模态输入回归
python3 verify_palette_interaction.py # 命令面板焦点/9 项编号/Esc 快照/颜色单元格回归
python3 verify_config_theme.py     # 配置体系：主题参考色板完整性 + keymap/theme C 单元测试
```


## 版本历史

**v2.1.8** —— 动画补上两处「看不见」的地方：切换标签页整页**左右滑入**，悬停气泡与底部提示条**淡入**；淡入的基准从「向黑压暗」改成「向页面底色混合」。

| # | 内容 |
|---|---|
| 切标签 = 整页横滑 | 用户：「切换标签也要有一个左右滑动的动画」。原先标签切换是瞬间换掉分屏树的根（`split.h`：切标签页只是换 root），0 帧过渡。现在按「新标签在旧标签的左边还是右边」定方向：往后的标签从**右**推进来、往前的从**左**进。做法是在交给帧差之前重写这一帧的正文：每条行定位（`ESC<行>;<列>H`）的列号加上本帧位移，落到屏外的格子直接不发 ⇒ 行宽永远不超过屏宽（不会折行溢出），命中的矩形、输入时序一个字节都没动。只有空格的铺底/清行 chunk 故意不参与位移，所以页面没推进来的那一截是干净的底色，不会露出上一页的残留。位移与亮度共用同一条缓动曲线、同一个 15ms 分片 ⇒ 收尾同帧，不会出现「亮到位了还在滑」。 |
| 淡入改成「向底色混合」 | 用户：「气泡的淡入完全没有看出来」。查下来是两件事叠一起：① 悬停气泡（`render_settings_tooltip`）与底部 toast 原先**一帧动画都没有**（状态键刻意排除 hover，它们按设计就是瞬现瞬隐），② 旧的淡入是「向黑压暗」，小浮层是实心底色画在页面上，压暗 25% 仍然是个完整的框。现在 `c = 底色 + (c - 底色) × 进度`：浮层/气泡第一帧几乎与页面同色，是真的「浮出来」。增益 1000 时该式是整数恒等 ⇒ 静止那一帧逐字节不变；只认 3 位十进制分量的写法，**没有新增字面量颜色**，`g_theme_refs` 不用登记。 |
| 气泡 / toast 只淡它自己那几行 | 状态键加两位「本帧画没画气泡 / toast」，于是从无到有的那一帧起一段 110ms 淡入；指针在行与行之间扫过去不重播（键不变 ⇒ 不会一路闪），消失仍是即时（淡出在终端里只会读成整屏闪一下）。动画期间只重写气泡/toast 占的那几行的颜色数字，其余行一格不动。 |
| 动画触发点上移到帧尾 | v2.1.7 的起动画挂在 `render_settings_panel` 出口，切标签时整帧可能根本没有面板 ⇒ 整段判断移到 `render_screen` 帧尾（`theme_remap` 之后，混合基准才是主题色）。分屏时也不会再出现「一个窗格的分支把状态冲掉 ⇒ 动画自我重启、屏幕永远留暗色」。`[general] anim = off` 时这些代码一帧都不执行。 |
| 回归判据 | `tests/verify_pane_palette.py` 的 **R 组**扩到 26 条判据（24×100 主档 + 12×100 / 24×40 两档，连多档一起跑出 28 行结果）：滑动不数像素，而是拿「整屏文本套一条闭式位移公式」找唯一解（方向与列数一起出来）；淡入用「`off` 与 `normal` 两次捕获的逐行颜色差集合」；「标签栏只跟着亮、不跟着滑」用「按行定位切段之后数可见字节」；再加一条「动画帧的字节流里不许出现 NUL」—— 动画会改写长度，长度与内容不吻合时缓冲尾巴会被当成光标段发给终端（画面看着正常，因为终端把 NUL 吞了，只有数字节才抓得住）。整套判据拿去跑 **v2.1.7** 的二进制 ⇒ 恰好且只有 R8 / R8b / R10b / R11 / R12 / R12b / R12c / R13 八条红；拿去跑本轮修 NUL 之前的那一版 ⇒ 只有 NUL 那条红。判据一律不读 C 的实现细节，也不放宽判据掩盖回归。没装 `libvterm-dev` 的机器（CI 的 macOS runner）上，要拿真实帧回放的屏幕级几条打 SKIP 而不是拿空帧假红、也不是拿 `None == None` 假绿；字节级的两条（CUP 列号、NUL）照常硬判 —— （`TERMUX_NO_VTERM=1` 在本机复现过一遍：SKIP 与硬判的分界正如预期）。`anim` 的毫秒数那条换了量法：不数「最后 12 帧里出现了几种颜色」——那会被采样窗口削平（CI 的 macOS runner 实测 7/10/10 三档撞顶），改成「一次翻页、整场窗口里数这一行压色压了几个帧」⇒ 帧间隔由机器决定，但帧数随时长单调增长与间隔无关（本机 60/110/200ms 读出 4 / 7 / 13 帧，正对 12ms 限流） |

**v2.1.7** —— 设置页的过渡动画：切页与浮层不再硬切，改成 ~110ms 淡入，`anim` 可关。

| # | 内容 |
|---|---|
| 为什么是「压暗再亮起」 | 终端没有半透明，也没法往正在绘制的帧里插帧 ⇒ 「淡入」唯一可行的做法是**把这一帧真的写给终端的颜色按一个增益压暗**，再用几帧回到原色。实现只挂在 `render_settings_panel` 的出口一处：状态键（当前页 + 哪个浮层开着）与上一帧不同就起一段过渡，然后把这条尾巴里出现过的 `\x1b[038;2;RRR;GGG;BBB` / `48;2;` 三元组按**所在行**的增益缩放（行号直接从尾巴里的 CUP 读）。缩放是**就地改那 9 个数字、长度一字不变** ⇒ 行尾对齐、限宽开窗、悬停气泡登记一概不动，也没有新增字面量颜色（`g_theme_refs` 的主题反查不受影响）；位数不是 3 位的写法直接放过，最坏是那一处不淡入而不是画花。 |
| 两种曲线 | 左栏入口点击、`F2`–`F5`、`Ctrl+↑/↓`、`Esc` 回启动页、命令面板跳页 ⇒ **整页均匀淡入**，起点 3/5 亮度（压到 1/4 会读成「闪一下黑屏」）。预设库 / 配色方案列表 / 十六进制编辑框出现时 ⇒ **自上而下逐行点亮**：每行一条自己的时间轴，越靠下越晚开始、但都在同一帧收尾，所以收尾不会齐刷刷跳一档。滚动**不加**动画（滚轮本来就是一格一格反馈，加了只觉得迟钝）。 |
| `anim` 开关 | `[general] anim = off \| short \| normal`（默认 `normal`；也可直接写毫秒数，上限 600）。`off` 时整段逻辑一帧都不执行 ⇒ 输出与没有这个功能时逐字节相同。动画期间靠 `needs_redraw` 把主循环的等待降到 8ms、渲染限流 12ms，实测约 8ms 一帧、110ms ≈ 7 档；亮度按 15ms 取整分片，同一片内的多余帧与上一帧逐字节相同，直接被帧差吞掉，不产生额外流量。 |

**v2.1.6** —— 设置页这一轮的六条反馈：滚动条改成**与终端原生滚动条一模一样**（靠近才出现、按距离渐变），左右两栏**全部交给滚动条管理**（不再钉住任何一行），侧栏去掉花色，条目行支持双击进编辑。

| # | 内容 |
|---|---|
| 条子外观照搬终端 | 用户：「滚动条要像正常终端的滚动条一样，有接近时的渐变等等」。设置页那三根条子（右栏纵条、面板底行的横条、极矮档侧栏那根细条）现在与终端正文**共用同一支画笔** `sb_paint_cell()`：指针进入条子 **10 格以内**才画、按距离走 `g_sb_grad[11]` 的 11 档渐变、滑块 = **整格换底色**（不再写 `█` 字符）、指针压在滑块上时整格反白、指针一走整条消失。`g_sb_grad` 与 `sb_custom_thumb/_track` 上移到文件前段，两处共用一份，`[theme] pane_scrollbar_thumb/_track` 的自定义色照样同时生效于终端与设置页。为此把终端侧原本内联的两处绘制（分屏 / 整屏）也改调这支函数 —— 画法是同一份代码，不存在"设置页自己一套样式" |
| 右栏整页横向滚动 | 「右侧太窄没有滚动条」的正解不是"把字挤小"，而是**让被裁掉的部分滚得回来**：右栏每一行先进一块**画布缓冲**（`settings_line_*` 发射器），再按横向偏移开窗写到屏上。画布宽度由本帧最宽一行的自然宽度**自动测出**（不给每页写宽度表），所以横向滚动条的行程与内容一字不差；行尾那三个 `...` 仍在（视觉线索），全文照样有悬停气泡。`Shift+滚轮` 与底行那根横条都能滚，两者共用 `g_settings_hscroll[]`。只有"画布比视口宽 6 列以上"才开横滚 ⇒ 80/100/120 列基准排版零改动 |
| 不钉任何一行 | 「加滚动条指的是完全由滚动条管理」——**左右两栏都是**。左侧栏把「导航选项 / 分隔线 / 启动 / 条目管理 / A / K / B / W / 保存」当成一个 9 行的列表整体滚动（以前「启动」永远占最顶端那行、`[Ctrl+S] 保存配置` 永远钉最后一行）；右栏同理：标题行能滚出顶端，提示行不再被兜到屏幕最后一行。行窗口标记 `(a-b/N)` 只画在提示行身上，提示行滚出屏幕就没有标记 —— 位置改由条子的滑块说明（与终端同一套语义） |
| `-1` 行号的残缺 CSI | 提示行不再钉底之后，"滚出可见区 ⇒ 行号 -1"这条路径第一次真的会被走到：`snprintf("\x1b[%d;%dH", -1, 91)` 生成 `\x1b[-1;91H`，vterm/终端把它当残缺 CSI，屏幕上直接掉出 `;91H(9-17/20)` 这种残渣（12×100 外观页实测）。现在 `settings_scroll_mark()` / `settings_hscroll_mark()` / 画布发射器 `settings_line_begin()` 三处一律拒绝行号 <1，外观页 hex 编辑那一行也自己看住行号 |
| 键位页在窄终端不再甩行 | 40 列时键位页能把 `改]]` 甩进侧栏（v2.1.5 的 `keys_widths()` 用 `avail = host_cols - main_left + 1` 分档，比发射器的真实预算**多算两列**）。口径改成 `vw = host_cols - main_left - 1`，并重新分配列宽：先给「当前键位」列 16（`Ctrl+B Shift+tab` 要 16 列），剩下的都归动作名 ⇒ 60 列起 `[改]`、80 列起 `[改][复位]` 全在视口内；实在挤不下（`vw < 24`）才整行摆到画布上交给横滚。表格单元格另加 `settings_line_text_w()`：长名字截到自己那一格，不再顶歪相邻列（50 列实测 `send-prefix` 压在「当前键位」上） |
| 侧栏去色 | 「条目管理不要不同的颜色和 hover 背景」——按澄清只改**左侧栏**：六个入口一律同色、无 hover 底色，当前在哪一页只由行首那一格 `▶` 说明（`▶` 是替换行首空格画的，宽度一分不增）。右侧条目表格的配色一字未动（`settings_row_style()` 两处共用，故侧栏另写一套，不碰表格） |
| 删掉重复的「默认：终端」 | 侧栏「启动」下面那行删掉 —— 它与条目管理页标题里的「当前默认：xxx」是同一件事。启动页那句「当前默认启动项：…」按澄清保留 |
| 双击进编辑 | 条目管理页里**双击**某一行（`[1] cmd cmd.exe` 那一块）= 直接进那一条的详细配置页；单击仍然只是选中（点在名字上不该顺手把配置页打开），落在 `[↑][↓][改][删]` 上照旧走按钮。启动页那张表同步（`handle_settings_mouse` 的 `DOUBLE_CLICK` 分支），Windows 控制台原生就给双击事件，POSIX 侧 `term_input_posix.c` 用「同按钮 + 同格 + 500ms 内」自己判定 |
| 提示行给标记让位 | 外观 / 键位 / 行为三页的提示行补上 `settings_line_reserve(settings_mark_room(...))`（此前只有条目管理页与启动页有）：行末那截 `(a-b/N)` 是另一支函数贴着右端补的，不让位就会压在最后一个宽字符上（v2.1.4 的老教训，这次是提示行跟着滚之后才暴露出来） |
| 行为页开关行不再甩进侧栏 | 顺手抓到一个真的越界 bug：`render_settings_behavior` 的开关行还是老式整串 `snprintf`（`%-22s` 键名 + **不限量** 的中文说明），窄终端上超出右边界就自动折行，折出来的那一截落在【下一行的行首】⇒ 24x50 实测 r7 开头变成「鼠标支持（标签点击 / 拖选」，把侧栏 `[A] 外观` 整行擦掉（`scrollback` 那行以前传的是 `host_cols + 1`，同样能顶到最后一列）。两处都改走画布发射器 ⇒ 装不下的部分交给横条 |
| 回归判据 | `tests/verify_pane_palette.py`：**Q 组重写为 10 条**并新增逐格背景色判据 `vt_cells()`（滑块是"整格换底色、不写字符"，只看屏幕文本会以为条子没画 —— 这条判据同时把"指针离得远就整条不画"钉死：Q1b/Q4b 两条反向判据）。新增"横条拖着走 = 亮块从左端挪到右端"，改判 K2/M1/M2（提示行不再钉底 ⇒ 标记只在滚到页尾时可见）、N1（侧栏「默认：」行已删 ⇒ 改判 `[M]` 紧贴启动行）、P 组 40 列（`»` 文字标记已被真横条取代 ⇒ 改判"行尾确实有省略号"）。合计 **139 条**全绿（v2.1.5 是 123 条）；老判据一条不红。另加 **14 条「侧栏那一段只允许侧栏自己的字符串」**不变量（7 个页面 × 40/50 列；顺带把「折行盖侧栏」这一整类从「查行宽」的盲区里捞出来 —— 拿 **v2.1.5** 的二进制跑同一套判据共 22 条红，其中 40 列键位页那条直接指出 `r22 = 「改]]    │」`，正是本轮截图里用户看到的甩进侧栏的那一截） |

**v2.1.5** —— 设置页太矮/太窄时右侧面板终于有了**真正的滚动条**（能拖、能点轨道、能左右滚），条目列表则从侧栏与启动页撤掉，全部收进「条目管理」一页。

| # | 内容 |
|---|---|
| 右栏纵向滚动条 | 用户反馈「终端太矮时右侧面板看不全，只能靠滚轮一页页翻，还不知道自己翻到哪了」。现在这一页的可见带被内容超过时，**终端最右一列**画轨道 `│` + 滑块 `█`，可以拖滑块一路到底、也可以点轨道按页翻；内容放得下就**整条不画** ⇒ 80/100/120 列基准零改动。画在「最右一列」是有意的：占用行 = 可见带的最后一行属内容区、标题行在 ≤40 列铺满、侧栏那一列是分隔线本身，三处都试出来有过预算代价，只有「纵条占 `host_cols` 列 + 横条占表头既有标记位」这一组不用多扣任何一格 |
| 标记让位 | 提示行右端的 `(a-b/N)` 以前贴着 `host_cols` 写 ⇒ 条子会把它收尾的 `)` 吃掉（12×100 实测）。改成「这一页要画条子 ⇒ 标记整体左移一格」，判据由 `settings_vbar_visible()` 单点提供（浮层开着时算 0），画条子与让格子共用同一句，不会「让了格子却没条子」或反过来 |
| 右栏横向滚动条 | 40~59 列那一档，表头右端原来只能看的 `«n/m»` 升级成**可拖的横条** `«█───»`（同一份 `SettingsBar` 几何），`Shift+滚轮` 照旧可用 |
| 滚到聚焦位置 | 用户：「聚焦时需要滚动到聚焦位置」。键盘 ↑/↓ 移聚焦行 ⇒ 行窗跟着夹（原本就有，M3/N1 现在测的是新的条目管理页）；**拖条子** ⇒ 条目管理页把聚焦行一起挪到窗口顶端（`settings_page_vset()`），否则条子一松手就被「聚焦行必须可见」那条夹子拽回去，看着就是「拖不动」 |
| 页尾按条目数算 | 条目管理页的可见带末行原先写死 26 行（`SETTINGS_MANAGE_LAST`），条目一多，**动作条与最后几行落在带外 ⇒ 滚多少下都到不了**。改成 `settings_manage_last()` = `12 + 条目数`，渲染、鼠标命中、滚动条三处同源；条子的行程同步放宽到整页（`settings_page_band_widen()`），不再只在「聚焦行附近」活动 |
| 启动页瘦身 | 「设为启动默认」这件事单独占一页太浪费：启动页现在只剩标题 / 说明 / 两个单选项 / 一行「当前默认启动项」/ 提示行（自然行 3..8），**不再列条目表**，任何终端高度都不会溢出（`settings_startup_last()` 从 `12 + 条目数` 改成常量） |
| 侧栏只留导航 | 按「左侧不要再额外放这些」，`[1] cmd` 那一整串条目从侧栏撤掉（连同它的窗口、计数与悬停气泡，删掉那段死代码 3.1 KB）。腾出来的行给底部那一排入口用：`[M] [A] [K] [B] [W]` 紧贴「默认：xxx」往下排，不再中间空一大段。右栏「哪几行要给侧栏让位」的门槛 `yield_row` 单独保留旧行位 —— 窄终端整页排版靠它，一格都动不得（56×18 的老教训） |
| 条目管理页 = 唯一入口 | 动作条从两段变三段：`[+] 新建条目` / `[P] 预设库` / `[设为默认]`；标签与段宽由 `settings_manage_action_label/_span()` 单点提供（渲染画它、鼠标命中也用它，v2.1.4 已为「画在一处、点在另一处」道过歉），命中不再写死 `14/29` 两档。页内 `S` 键 = 把聚焦那条设为启动默认（没聚焦就取第一条，按钮不会是死的），已是默认则不写盘；标题行同步显示「当前默认：xxx」 |
| 极矮档侧栏入口窗口 | 9 行以下的终端里侧栏那一排入口摆不下，老版 `break` 掉画不下的 ⇒ **`[W] 窗格配色` 直接消失，用户再也进不去那一页**。现在按「窗口 + 一根细滚动条」处理：能画几条画几条，条子画在分隔线那一列（`sb_w-1` 是长标签的落点，会啃掉 `[Ctrl+S] 保存配置` 的收尾），滚轮（指针在侧栏）与拖条子都能翻；正常屏高一行都不受影响 |
| 截断还能查全文 | 条目从侧栏撤掉后，长名字只剩右侧表格这一处 ⇒ `render_menu_rows` 给**名称列与命令行列**各登记一条悬停气泡（宽终端那档以前没有），拷贝停在字符边界上（按字节截断会留下半个 UTF-8 序列，渲染成 `?`，框宽还差一列） |
| 回归判据 | `tests/verify_pane_palette.py` 新增 **Q 组** 8 条：12 行条目管理页最右一列有轨道+滑块；拖到底 ⇒ 动作条 `[设为默认]` 露出来且滑块在轨道下端；点轨道下端能翻页；24×80 基准档最右列干净；40 列表头横条滑块贴左端 + 拖到右端；9 行极矮档侧栏细条子 + 滚轮翻出 `[W]`；连按 ↓ 行窗跟着聚焦行走。M3 / N1(3 条) / N3 / N7 的判据随页面搬迁改写（悬停目标行列由屏幕文本反查，不写死行号），P 组的纵向标记不再写死 `(3-23/24)` 而是比对「横滚前后逐字相同」。拿 **v2.1.4** 的二进制跑同一套判据 ⇒ 只有 v2.1.5 新增/改写的这些变红，老判据一条不红 |

**v2.1.4** —— 设置面板右栏在窄终端可以横向滚动，并把条目的增删改收进新的「条目管理」子页。

| # | 内容 |
|---|---|
| 右栏横向滚动（Shift+滚轮） | 40~56 列的终端里，右栏视口只有十几到三十列，「名称列 + 命令行列 + 四个按钮」根本摆不下：以前只能把列一压再压，名称被截成`日常…`、命令行整个消失。现在表格按**虚拟画布**排版（名称 12 + 命令行 30 + `[↑][↓][改][删]`），视口只画其中一段 ⇒ **按住 Shift 滚滚轮即可左右滚**（普通滚轮仍是纵向翻页，互不打扰），行尾用 `«n/m»` 标出横向位置。只有「画布真的比视口宽」的页面才启用（启动项表 / 条目管理表），且 `host_cols ≥ 60` 一律不启用 ⇒ 80/100/120 列基准逐字节零改动 |
| 聚焦自动滚到聚焦位置 | 选中行变化（↑/↓、滚轮、点击）时，把「序号 + 名称」那一段拽进视口：已经看得见就一动不动，看不见才滚；只在**选中行刚变化那一帧**动手，否则用户自己滚出来的位置会被每帧拽回去。名称列滚进视口后按钮让位（按钮钉在视口右端，滚到最右又能拿回来） |
| 新增「[M] 条目管理」子页 | 用户反馈「启动项页一行里塞了 `[+] 添加条目`、`[P] 快速预设库` 和每行的 `[↑][↓][改][删]`，表只剩三列能看」。现在**增删改与排序全部收进独立子页**（列表 + 每行 `[↑][↓][改][删]` + 顶部 `[+] 新建条目`/`[P] 预设库`，`Ctrl+↑/↓` 调序、`X` 删除、`Enter` 进详情），侧栏只留一个 `[M] 条目管理` 入口；「启动」页那张表改为**只读**（只保留「设为启动默认」单选与 ←/→/Space），少一行按钮 ⇒ 同屏能多看两行条目。启动页的 `Enter/E/M/数字键` 直接带去条目管理页选中项，双击仍进该条详细配置 |
| 窄终端的让位阶梯 | 视口放不下时按固定顺序退，而不是随手裁：丢命令行 → 丢掉列间隔 → 收窄名称列（12/8/4）→ 丢 `[↑][↓]` → 连 `[改][删]` 一起丢（退成纯键盘，靠 `M`/`E`/数字键操作）。实测（24 行、条目管理页）：≤46 列「序号 + 名称」，48~58 列加回 `[改][删]`，≥60 列四键 + 命令行全开。列的定义只由 `menu_geom()` 给一次（先按画布定列宽，再按视口挪按钮），表头、表体、鼠标命中三处共用 `settings_menu_table_calc()` ⇒ 不会出现「按钮画在一处、点在另一处」 |
| 行尾标记不再压字 | 提示行右端的 `(a-b/N)` 和表头右端的 `«n/m»` 是**另一支函数**贴着右端补上去的：窄终端下正文被裁到行尾时，标记正好压在最后一个**宽字符的右半格**上，vterm 把那个汉字挤成半角 ⇒ 整行显示宽度反而多出 1 列（32/50/56 列实测会顶到自动折行）。现在标记的落点由 `settings_mark_left()` 一处决定，正文写之前先问它要「让几列」；一列都放不下就整条提示不写（宁缺勿叠）。详情页/条目的动作条文案也收短了（`[+] 新建条目`、`[P] 预设库`） |
| 回归判据 | `tests/verify_pane_palette.py` 新增 **P 组**：40 列（纯键盘档）与 56 列（有按钮档）各验「不折行、不盖侧栏、Shift+滚轮真的改变画面、纵向行窗口标记不变」；O/K/N/M 组的入口按键随新页面改掉（`[改]` 的点击改从条目管理页进，详情页经 `[M]` → `Enter` 进）。把整套判据拿去跑 **v2.1.3** 的二进制 ⇒ 恰好且只有 K1、N1 与 3 条 P 变红（新行为的判据确实咬得住，不是放宽判据掩盖回归）。另在 `cols=28..60 × rows=10/12/18/24/30 × 6 个子页 × 滚轮 0/3` 全档校验「没有任何一行超过终端宽度」；P 组的判据全在 libvterm 回放的真实帧上比列宽，没装 `libvterm-dev` 的机器（macOS runner）明确 SKIP 而不拿空帧假红，条目 ini 里的长路径也改成临时目录，不再写死某台机器的绝对路径 |

**v2.1.3** —— 窄终端（40/50 列）设置页左右两栏互盖、分隔线消失。

| # | 内容 |
|---|---|
| 40 列左右栏互盖 | 用户反馈「终端只有 40 列时，设置页左右两栏互相盖住，中间那条分隔线整段没了」。**右栏**：启动项页的表格（表头/名称列/命令行列/按钮）绕过行宽预算，可用宽度按 `host_cols - main_left + 1` 算（比真实值多 2 列）⇒ 整行超出终端宽度 → 终端自动折行 → 把**下一行的行首**连侧栏条目一起盖掉。**侧栏**：`[A] 外观 / 主题`、`[K] 键位设置`、`[B] 行为开关`、`[W] 窗格配色`、`[Ctrl+S] 保存配置` 这批 18~22 列的写死定宽串，尾部补齐的空格正落在分隔线那一列上，把 `│` 擦成空格。现在表格几何（表头/名称列/命令行列/按钮列/降级档位）统一由 `menu_geom()` 一处决定，名称列**要么整块放下、要么整列丢掉**，不再为塞按钮把它压成 4 列；窄到放不下就依次丢 `[↑][↓]` → 命令行整列 → 按钮（键盘热区仍然可用） |
| 行宽预算 | `settings_line_*` 的段预算原先只记「旧预算 − 段宽」，被裁短的段末尾那格补位空格等于凭空多写 1 列 ⇒ 同样触发折行；右侧正文宽度也统一收到 `host_cols - 1`（最后一列必须留白）。右栏每一行再按左侧栏的实际位置二次限宽：正好压在侧栏底部 `[A][K][B][W]`/保存行上的那些行，正文只允许写到分隔线之前（一列都塞不下就整行不画，滚轮仍能滚进来）。宽终端（`host_cols ≥ 60`）一律不挡 ⇒ 80 列基准逐字节零改动 |
| 热区与画面同源 | 启动项页两个单选项原先按写死的 `main_left+26` / `+29..+51` 判定命中：40 列时「内置帮助」根本没画出来，点那块空白却会改配置，而且 `+51` 已经越过右边界 ⇒ 改为 `settings_startup_radio_spans()` 与渲染共用一份宽度。表格 `[改][删]` 的命中也补上 `settings_menu_show_btn` 门控（按钮被几何降级丢掉时不再吞点击）。行为页 scrollback 那一行的 `[-] 值 [+] (对之后新建的 pane 生效)` 从固定列位改成随行宽预算，窄终端依次丢括注 → `[+]` → `[-]`，不再冲进侧栏 |
| 行窗口标记 | 滚动时贴在行尾的 `(a-b/N)` 位置标记原先也一律贴着 `host_cols` 写 ⇒ 14x30 实测它把侧栏 `[K] 键位设置` 的后半截和 `│` 一起盖掉、最后一个字符被挤出屏幕（`(3-5/12)` 落在 col 36..43）。现在同一套逐行限宽也管它，宽终端不受影响 |
| 回归判据 | `tests/verify_pane_palette.py` 新增 **O 组**（40/50 列 × 6 个子页）：每行显示宽度不得超过终端宽度，且内容越过侧栏宽度的行必须在侧栏列上仍是 `│`。`verify_alignment.py` 对表格两列补齐的判据同步改为「宽度来自 `settings_menu_name_w` / `settings_menu_cmd_w`」。另外在 `cols=18..140` 全档校验菜单表几何总宽不溢出 |

**v2.1.2** —— 设置页侧栏可滚动 + 窄页省略号 + 浮层右框对齐。

| # | 内容 |
|---|---|
| 导航选项可以滚 | 用户反馈「设置中导航选项要可以滚动，而不是省略成 添加(共4项)」。侧栏的菜单项列表原先被截成固定窗口，第 N 项以后永远看不见，`[+]` 那行还被改成「添加(共N项)」这种点不动的死提示 ⇒ 列表改为**可滚动窗口**：滚轮先滚侧栏、滚到头才带动右侧表格的选中项，`PgUp/PgDn` 同样滚窗口，窗口与右侧选中项同步夹取（右侧换行、侧栏跟着挪）；`[+]` 行恢复普通的「添加新条目」，还剩几项只在**真能滚**时由表头 `(a-b/N)` 说明；条目少于窗口容量时窗口按条目数收窄，`[+] [P] [A]…` 紧贴列表末行，侧栏不再空出一段 |
| 窄页标签没有 `…` | 用户反馈「`[用此项新建标签页时的颜色，默认=蓝]` 太窄时没有 … 提示」。菜单项详细配置页里 `3. 启动目录…` / `4. 启动默认颜色…` 两行标签和三个按钮行是裸 `snprintf`，窄终端下把后半个汉字硬切成乱码、按钮行还会溢出到下一行 ⇒ 统一改走 `settings_line_begin/text/text_e/end`，装不下就以 `...` 结尾，按钮行用 `ellip=0`（宁可整段不画，也不留半截命中区） |
| 侧栏悬停提示 | 用户反馈「光标在导航选项中时可触发旁边的悬停提示」。原先侧栏那行借用右侧表格的命中区：鼠标停在侧栏 ⇒ 弹出**右边窗格**的气泡（误触发），而被截断的菜单项名自己没有提示 ⇒ 侧栏逐项登记命中区（只有截断才登记，全文放得下时不弹），命中判定去掉「同一行兜底」，气泡落在源行上时整体推到分隔线右侧的正文区（顶/底框/正文同一列，且不把侧栏各行顶掉）；侧栏名字改为按显示宽度手工截断并补 `...`（不再借用 `%-10s` 式定宽截断） |
| 配色方案右框 | 用户反馈「窗格选择的配色方案右侧没有对齐」。方案浮层的列表行给名称列多记了 1 列，右框 `│` 比顶/底框的 `┐ ┘` 短一格 ⇒ 行内宽度与框宽同源，用屏幕列号 dump 实测 `│@72` 与 `┐@72` 同列 |
| 颜色编辑右框 | 用户反馈「颜色编辑弹出的框右侧没有对齐」。底行的横线从第 1 列起算、顶行从第 0 列起算 ⇒ 右下角 `┘` 比 `┐` 短 1 列（60/44/100 列都差一格）。统一为从 0 起算，并把值行末尾的提示语按同一个框宽补齐 |
| 名字装不下 | `ChooserItem.name` 只有 32 字节 —— 11 个汉字就被切，**保存配置时还把切过的结果写回 ini**（用户看不到任何提示地丢字）⇒ 提到 64 字节，编辑缓冲 `g_edit_name` 与之同源（不再写死 31）。顺带修气泡正文：按字节 `snprintf` 截断会把半个 UTF-8 序列留在串尾（显示成 `?`，框宽也跟着差一格），改为按字符边界逐字拷贝 |
| 判据 | `verify_pane_palette.py` 新增 N 组：12/13 行下侧栏窗口滚进第 4/5 项、24 行下表头不带 `(a-b/N)` 且 `[+]` 紧贴列表末行；50 列详情页四个字段标签都在、`...` ≥ 3 处、按**显示宽度**（`east_asian_width`）逐行 ≤ 50；24×100 悬停给全文且气泡不盖侧栏、停在侧栏空白处不弹邻居气泡；浮层右框用新增的 `VTERM_MEAS_C`（libvterm 回放的**屏幕列号** dump）判 `┐/┘/│` 同列——文本行对比看不出一格之差，之前就是这么漏掉的；60 字节名字 Ctrl+S 后 ini 里仍是完整 20 个字。在 v2.1.1 基线上跑：N1/N3/N5/N7 + K1 共 11 条红，修后全绿（K1 那条原先断言的「添加(共5项)」文案 v2.1.2 已有意去掉，判据同步改为「不再出现死提示」）。`make unittest` 355 项、`lint-o1` 双侧 0 警告、clang `-B linux` 0 诊断 |

**v2.1.1** —— 颜色编辑浮层 + 设置页滚轮 + 色块条限格。

| # | 内容 |
|---|---|
| 过窄时颜色编辑被截断 | 用户反馈「过窄时，颜色编辑被截断」。十六进制编辑框原先嵌在表行里（外观页 `role_col + 21`、窗格页 `col + 22` 起笔），终端一窄整段就跑到屏幕外——看不见色块预览，也看不见自己敲到哪一位。现在按 `Enter`/点色块后弹出**居中的「颜色编辑」浮层**：色块 + 完整 8 位（`#rrggbb`）+ 操作提示，框宽取「正文区放得下多少给多少」，光标直接落在浮层的值段上；正文区窄到连框都放不下时才退回表行内联。此时表行不再重复画编辑框（同一个值不会出现两份） |
| 矮终端设置页无法滚轮 | 用户反馈「终端过矮时，设置右边窗格无法滚轮滚动」。设置页的鼠标处理函数开头就是「不是按下就 return」，滚轮事件整个被丢掉 ⇒ 现在滚轮按页翻滚动量（启动项页 / 外观页 / 键位页 / 行为页 / 窗格配色页 / 菜单项详细配置页各持一份，与 `↑/↓` 用的是同一套滚动量），选中项跟着页面整体平移（和命令面板滚轮的手感一致）；「常用命令行预设」与「窗格配色方案」浮层里滚轮改为移动高亮。hex 编辑中滚轮会先收下当前值（满 6 位即写盘）再滚页 |
| 色块条限格 | 「启动默认颜色」选择条固定 30 列（默认 6 + 8×3），窄终端右侧区放不下时后几格既看不见也点不到 ⇒ 改为按可用宽度决定画几格（行尾留 2 列写 `+N`），渲染与鼠标命中用同一个数；剩余色块仍能用 `←/→` / 数字键取 |
| 判据 | `verify_pane_palette.py` 新增 M 组（12 行外观页滚轮把语义色区滚进可见区，且提示行的行窗口 `(a-b/20)` 起始行随之后移（12 行下不滚动也带 `(3-11/20)`，所以比的是数字、不是「有没有标记」）；滚到底再滚回来必须逐字回到基线那一屏；启动项页第 3 项、详情页第 3 个字段、窗格页最后一项「亮白」滚轮后可见；12×30 / 12×44 / 24×60 三档窄终端下编辑浮层完整显示名称与 `#ab`；浮层光标落在值段之后；40/60/100 列详情页色块条限格与 `+N`）。`verify_item_color.py` 的独立 harness 改调 `render_item_color_row_w(...)`（宽度作参数）并 +1 项限格自证。`make unittest` 355 项、`lint-o1` 双侧 0 警告 |

**v2.1.0** —— 配色方案可挑选 + 截断悬停提示 + 矮终端不裁内容。

| # | 内容 |
|---|---|
| 方案可选 | 用户反馈「窗格配色要可以选择」。v2.0.9 的方案行只有 `‹ 名字 ›` 与 ←/→ 逐个换：看不见有几套、也看不见长什么样。现在 `Enter`（或点行右侧）弹出**方案列表浮层**：8 套方案逐行列出，每行带 8 个**实际色块预览**（背景 / 前景 / 滚动条 / 轨道 + 前 4 个 ANSI 色），`↑/↓` 或数字键选、`Enter` 应用并写盘、`Esc` 只关浮层；鼠标直接点某一行即选中并应用。←/→ 快速切换保留在方案行上 |
| 太窄用 `...` | 用户反馈「有些地方太窄会显示不了」。自适应布局能砍列、能压宽，但砍到极限仍会有装不下的文字（如 60 列下的提示行、窗格页的值段）。现在这些行**就地截断并在行尾留 `...`**，鼠标停在上面时以**跟随鼠标的浮层气泡**给出完整内容（超宽自动折行，最多 3 行）；浮层不遮挡正在输入的状态（编辑 hex / 录制键位 / 其它浮层打开时不弹） |
| 太矮显示不全 | 用户反馈「终端太矮时设置显示不全」。两处根因：① 侧栏是固定行号，12 行终端下 `[A]/[K]/[B]/[W]` 四个入口被挤到屏幕外，连子页都进不去 ⇒ 侧栏改为按高度自适应（先省表头与分隔行，再省 `[P]` 行，菜单项列表按剩余空间截断并在 `[+]` 行标出总项数），四个入口与 `[Ctrl+S]` 永远在屏内；② 外观页（22 行）、行为页、菜单项详细配置页、启动项页的行号是写死的，超出部分直接画到屏幕外 ⇒ 四页各自支持**纵向滚动**，`↑/↓` 越过可见边界时整页跟着走，提示行右端标出行窗口 `(3-11/20)`；渲染与鼠标命中共用同一批行号函数（含十六进制编辑光标，滚出可见区就隐藏光标） |
| 判据 | `verify_pane_palette.py` 新增 J 组（浮层 8 个方案名 + 方框 + 每行色块预览 ≥32；数字键 6 应用 GitHub Light → ini 20 键且 `pane_background = #ffffff`；`Esc` 不写配置；浮层内按键被吞掉不串到槽位表）、K 组（12 行终端下四个入口与保存行都在屏内；5 个菜单项时 `[+]` 行标出总数；外观页连按 ↓ 后语义色区滚进可见区并带 `(a-b/20)` 标记；键位页滚到底末尾动作可见、第 2 个已滚出）、L 组（60 列下提示行确实出现 `...` 且尾部文字不可见 → 悬停后气泡里能看到尾部文字且有框线、气泡贴着鼠标行；100 列不截断则不画气泡）。`verify_settings_ui.py` 补 9 条同源锚点，`make unittest` +5（方案 preview 与 apply 逐项一致、只读不改配置、越界返回全黑）。v2.0.9 二进制跑这套判据：**14 红** |

**v2.0.9** —— 窗格配色预设方案 + 窄终端自适应 + 编辑时光标。

| # | 内容 |
|---|---|
| 预设方案 | 「窗格配色」页顶部新增一行方案选择：`‹ 名字 ›` ←/→ 换、Enter 一键应用（立即生效并写入 ini）。内置 **Campbell / One Half Light / One Half Dark / Solarized Light / Solarized Dark / GitHub Light / Dracula / Nord** 八套，每套一次写满 20 个槽位（滚动条滑块取该方案的亮黑、轨道取背景），应用后仍可逐项改。正中方案时标 `● 已应用`。想要浅色窗格：选 GitHub Light 回车即可 |
| 窄终端 | 用户反馈「太窄会显示不了」。窗格配色页从固定两列改为**按宽度自适应**：装不下两列就单列显示（+ 滚动，右下角标 `(n/20)`）——v2.0.8 在 60 列下右列会折行盖到侧栏和底栏上。所有设置子页的说明/提示行按可用宽度裁剪，并在画设置页时关掉终端自动折行（`?7l`），内容超出就地截断而不再绕行。键位页在窄屏先砍「说明」列、极窄时只留 [改]（[复位] 用 R 键），菜单项页压窄「启动命令行」列、极窄时只留 [改][删]（调序用 Ctrl+↑/↓）——按钮不再被裁到屏幕外 |
| 光标 | 用户反馈「编辑时光标不对」。窗格配色页编辑十六进制时，光标定位代码仍在用**外观页**的角色行/列（角色号是负的槽位编码）⇒ 光标飞到别的行。改为按窗格页自己的行列计算，并对滚出可见区的项隐藏光标 |
| 判据 | `verify_pane_palette.py` 新增 H 组（方案应用 → ini 20 键 + 窗格文字白底；编辑光标紧跟 `#ab` 之后、右列预填 6 位之后；60 列单列滚动后「亮白 (跟随终端)」完整可见）与 I 组（60 列 × 24 行下窗格/键位/菜单项/行为四页的按钮都在屏幕内、且所在行左侧侧栏没被折行内容占掉）。`verify_settings_ui.py` / `verify_alignment.py` 的锚点同步到新的自适应几何函数。`make unittest` +6。v2.0.8 二进制跑这套判据：14 红 |

**v2.0.8** —— 滚动条颜色可自定义 + 窗格配色页中文标签对齐。

| # | 内容 |
|---|---|
| 滚动条颜色 | 用户反馈：设了浅色 `pane_background` 后滚动条「和背景相同」——内置的滚动条渐变是深色系写死的。新增 `[theme] pane_scrollbar`（滑块）/ `pane_scrollbar_track`（轨道底色），整屏与分屏两条渲染路径都改；设了就用恒定色覆盖渐变，轨道「│」字色取轨道与滑块的中间值保证可见；不设时字节不变。设置页「窗格配色」左列第 3、4 行即可改（未设显示「(内置渐变)」） |
| 对齐 | 窗格配色页标签用 `%-16s` 按**字节**补齐，中文标签宽度不一致 ⇒ 「默认前景(字色)」那行的值比其他行偏 2 列。改用 `append_padded_utf8` 按显示宽度补齐 |
| 判据 | `tests/verify_pane_palette.py` 新增 F 组（悬停右缘后滑块发 `48;2;255;0;0`、轨道发 `48;2;0;0;255`，未配时不出现；只配滚动条不影响文字透传；设置页改「滚动条滑块」写入 ini）与 G 组（libvterm 回放整页，左右两列值起始列各自唯一——v2.0.7 二进制为 `{41,43}`/`{78,79}` 红，新版 `45`/`81` 绿）。`make unittest` +4 |

**v2.0.7** —— 设置页新增「窗格配色」子页：`pane_background` 与红/绿/黄/蓝/紫/青/白等 18 项全部可在界面里改。

| # | 内容 |
|---|---|
| 窗格配色页 | 侧栏第六项 `[W] 窗格配色`（启动页 `F5`/`W`，命令面板 `pane-palette`）。左列：默认背景、默认前景、黑…青；右列：白、亮黑…亮白。每项显示色块 + 当前 hex，未设置显示「跟随终端」。`Enter` 复用十六进制编辑框（`g_hex_edit_role` 用负数编码窗格槽位，不动 16 个 UI 角色的逻辑）；`R` 复位单项、`Ctrl+R` 清全部；每次确认都 `save_config()` 落盘并即时重绘 |
| 顺手修 | hex 编辑框进入时预填当前值 6 位，原来必须先退格才能输入；现在第一次打字自动整段替换，退格则保留剩余位（外观页的语义色编辑同样受益） |
| 判据 | `tests/verify_pane_palette.py` 新增 E 组：真 PTY 里 `Ctrl+B s → W → Enter → ffffff → Enter`，断言 ini 出现 `pane_background = #ffffff` 且窗格普通文字底色发 `48;2;255;255;255`；`↓×3 → Enter → ff0000` 只写 `pane_red`、红字发 `38;2;255;0;0`；`R` 复位后 ini 不含该键且回到 `;40`。v2.0.6 二进制跑 E 组 4 条红（无该页），新版全绿。Windows/Linux `-Wall -Wextra` 0 警告 |

**v2.0.6** —— 修 **bug #32**（Windows 点 × 关标签永久卡死）+ 新增窗格 16 色 palette（`[theme] pane_*`）。

| # | 内容 |
|---|---|
| bug #32 | **现象**：Windows 上点标签的 × / `Ctrl+B x` 后整个 termux 无响应。**根因**：跨平台移植（`16a38b9`）改写 `close_pane()` 时把顺序弄反、还漏掉了 `ClosePseudoConsole`：变成 `join(读线程, 2000ms) → CloseHandle(管道) → hpc = NULL`。读线程阻塞在 `ReadFile(pipe_out)` 上，conhost 没被关、写端一直被它持有 ⇒ join 必白等 2 秒；随后对**正被另一线程阻塞读的管道句柄** `CloseHandle`，在 Windows 上会把 `CloseHandle` 自己也挂住 ⇒ 主线程永久卡死。**修法**：恢复移植前顺序 `ClosePseudoConsole → CloseHandle(管道) → join`，并补关 `pi.hThread`（原来只置空，句柄泄漏）。POSIX 分支不变。**限制**：本机无 Windows、CI runner 上 ConPTY 子进程活不下来（见 v2.0.3），此修复**只有代码推理与移植前版本的对照**，没有运行期证据，需真机确认 |
| 窗格 palette | **现象**：改 `[theme] background` 后 cmd 背景仍是黑的。**原因**：`background` 等 16 个角色只管 termux 自己的 UI；cmd 的普通文字是 16 色索引属性（`0x07`），渲染时原样发 `\x1b[0;37;40m`，宿主终端按自己的 palette 画 `40` = 黑，termux 的主题根本看不到它。**新增**：`[theme]` 段的 `pane_foreground` / `pane_background` / `pane_black` … `pane_white` / `pane_bright_black` … `pane_bright_white` 共 18 个键（像 Windows Terminal 的 color scheme），渲染 16 色属性时改发真彩色；**一项都不设时输出一字节不变**。`Ctrl+B r` 热重载生效，`Ctrl+S` 保存不丢。设置页暂无编辑器（角色编辑器绑死在 16 个 UI 角色上），下一版补 |
| 判据 | `tests/verify_pane_palette.py`（`make palette-posix`，进 `check-posix`）真 PTY 起 termux：A 无配置时仍发 `;37;40`；B 配白底深字后发 `48;2;255;255;255` / `38;2;36;41;47`；C `pane_red` 让 SGR 31 发 `38;2;255;0;0`；D libvterm 回放那一行背景真的是 `(255,255,255)`。修前二进制 B/C/D 五条全红。**C 项在开发中抓到一个真 bug**：屏幕属性存的是 Win32 位标志（RED=4）而 `pane_*` 按 ANSI 编号（red=1），第一版没换算，`pane_red` 落到了 `pane_blue`。`make unittest` +13 条覆盖 theme 层 API 与 identity 不被打破 |

浅色窗格示例（写进 `termux.ini`，`Ctrl+B r` 重载）：

```ini
[theme]
pane_background = #ffffff
pane_foreground = #24292f
pane_black = #24292f
pane_bright_black = #57606a
```

**v2.0.5** —— 修 **bug #31**：alt 屏全屏程序（nano / vim）铺满整宽时最后一列显示为空白（Linux 真机报告）。

| | |
|---|---|
| 现象 | termux 里 `nano` 打开 80 列宽文件，每行最后一个字符没了、显示成空白；直接跑 nano 正常 |
| 机理 | 整屏路径每行画完正文后无条件补 `\x1b[0m\x1b[K` 清行尾（v1.8.37 为关闭分屏窗格后右侧残留而加）。正文已写满宿主整宽时，宿主终端光标处于「延迟折行挂起」态、逻辑上仍在末列，`EL(0)` 从末列起清，把刚写的那一格擦掉。**各家终端不一致**：tmux / pyte 保留末列，libvterm（neovim、不少 GUI 终端的内核）擦掉 |
| 修法 | `render.c` 整屏路径：`text_rc < host_cols` 才发 `\x1b[K`；满宽时右缘本就没有残留可清，只发 `\x1b[0m` |
| 判据 | 新增 `tests/verify_lastcol.py`（`make lastcol-posix`，已进 `check-posix`）：真 PTY 起 termux + 模拟满宽 alt 屏程序，① 字节级：满宽行后不紧跟 `\x1b[K`；② 语义级：libvterm 回放后第 80 列是 `E`。修前二进制两条都红（EL 命中 23 处、末列 `' '`），修后全绿。v1.8.37 的场景（关右窗格后左窗格扩宽无残留）复测仍通过 |
| 分屏路径 | 不受影响：`render_split_pane` 逐格铺底不用 `\x1b[K`，分屏里 nano 末列本来就正常（已实测） |

**v2.0.4** —— 第六轮外部审计（Linux 真机 + ASan）：修 **BUG-12** 键名截断，加固 `split_layout` 契约，新增 `-O1` 编译门禁。

审计结论先说：ASan 版二进制跑 5 窗格 + 30 次随机 resize + 灌 2000 行 + 搜索 / 复制 / 设置页 / 逐个关窗格，**零报错**；`make check-posix`、`verify_all.py` 全绿。本版修的是审计找出的唯一真问题和两条加固建议。

| # | 问题 | 修法 | 判据 |
|---|---|---|---|
| BUG-12 | `spec_text` 最长产出 `Ctrl+Alt+Shift+backspace` = 24 字节，`keymap_describe` 内部 `prefix[24]`/`key[24]` 差 1 字节装不下 NUL，帮助页 / 命令面板显示成不存在的 `…backspac`。设置页录键走不到（不追加 `S-`），但手写 `termux.ini` 的 `prefix = C-M-S-backspace` 直通，且 ini 注释就教了这个语法 | `keymap.c` 缓冲 24→40；`render.c` 帮助页 `combo`/`prefix`/`key` 统一 64、`keycol` 80；`g_split_shortcut_buf` 24→64；`pane.c` `close_key` 48→64 | `make unittest` 新增 3 条：改前 `got "…backspac …backspac"` 验红，改后 325 checks 0 failed |
| 契约 | `split_layout` 的 `layout_rec` 在空间不足时 `if (total < 2) total = 2;` 把总量**撑大**而不是收缩，W=1 的父矩形算出右界 3 的子矩形。下游渲染有裁剪所以用户看不见（真 PTY 4 窗格缩到 1×1 再恢复全程存活），但纯函数违反「子矩形 ⊆ 父矩形」 | 改成 `if (total < 0) total = 0;`（收缩） | `verify_split.py` 新增 #30 穷举 23040 个子矩形（越界=0 / 负尺寸=0）+ 自证 5（退回旧写法必须重新变红） |
| 门禁 | GCC 的 `-Wformat-truncation` 在 `-O1` 与 `-O2` 下诊断集合不同，生产/CI 都是 `-O2` 所以从没见过 | 新增 `make lint-o1`（Windows 源码 mingw 交叉 + POSIX 源码本机 cc，只 `-c`、`-Werror`）并接进 Linux CI 作业 | 门禁刚建起来就抓到 `config.c:121`（basename 255 → name 32）和 `pane.c:413`（命令行 512 → 标题 256）两处隐式截断，均改成显式 `%.*s`；退回 `config.c` 修复门禁必须变红 |

**没做的**：审计中期建议 4（极窄时自动只显示活动窗格）、5（清掉 4 个纯镜像测试）、6（会话恢复）留待后续版本。

**v2.0.3** —— 修 **bug #30**：标准输出句柄只写时 termux 直接拒绝启动。

`GetConsoleScreenBufferInfo` 要求句柄带 `GENERIC_READ`，而 `GetStdHandle(STD_OUTPUT_HANDLE)`
拿到的常常是【只写】句柄 —— 这时它返回 0、`GetLastError() = ERROR_INVALID_HANDLE (6)`，
termux 就在启动第一步打印 `cannot query console buffer` 然后 `return 1`。

在 ConPTY 下这是**必然**发生的：子进程拿到的标准 I/O 是 ConDrv 上通用的
`Input`/`Output` 句柄，而不是 `CONIN$`/`CONOUT$`。所以任何把 termux 挂在伪控制台下的
宿主（终端复用器、CI、远程会话）都会看到它一闪就退。

修法是启动时查询失败就另开一个 `CONOUT$`（读写都有）并改用它；输入侧对称补 `CONIN$`
兜底 —— 否则 `SetConsoleMode` 会**静默**失败，表现是「程序在跑但收不到任何按键」，
比启动就报错更难查。两个自己开的句柄在退出时关掉，`GetStdHandle` 拿来的不关。
在源头换掉 `g_mux.hOut` / `g_mux.hIn`，一次覆盖全部三个调用点：

| 位置 | 原来的后果 |
|---|---|
| `src/main.c:43` | resize 时静默 `return` —— 等于 resize 检测失效 |
| `src/main.c:171` | 启动失败（本次暴露的那条） |
| `src/platform_win.c:96` | `plat_console_size` 直接返回 -1 |

普通控制台窗口下标准输出句柄本来就可读，这段分支**不会进入**，现有 Windows 行为不变。

这一版还把 Windows 侧的诊断能力补上了 —— 此前 termux.exe 在真 Windows 上的运行期行为
**完全没有观测手段**，每猜一次要等 CI 3~4 分钟：

- `dump_mark()`（`src/globals.c`，只在设置了 `TERMUX_DUMP` 时生效，正式使用零影响）
  记录启动每一步、ConPTY 送来的每个 `KEY_EVENT`、子 shell 的 spawn 结果与退出码。
- `tests/win_smoke.py` 增加**对照实验**：用同一个驱动直接起裸 `cmd.exe`，
  用来一刀切开「termux 有问题」和「环境有问题」。

**★ 已知限制（不藏着）**：GitHub Actions 的 Windows runner 上 ConPTY 子进程绑不到
伪控制台 —— 裸 `cmd.exe` 结果完全一样（85 字节全是 conhost 自己的初始化，
cmd 本体零输出，横幅跑到了宿主控制台），所以这是**环境限制而非本项目的 bug**。
后果是子 shell 立刻退出、窗格被回收、termux 跟着退出，于是关于页 / 分屏 / resize
这 9 项在该环境下记为 `[SKIP]` 并打印原因，**不是**悄悄算通过。判据是活的：
换到有真交互控制台的机器，对照通过后这 9 项会自动变回硬断言，不需要改代码。

该环境下确实拿到了运行期证据的部分：进程能启动（bug #30 修复后）、渲染出帧、
帮助页出现、帮助页平台串是 Windows 的（**bug #9 首次拿到运行期证据**，
此前只有 `gcc -E` 预处理级证据）、没有串成 Linux / macOS。

**v2.0.2** —— 仓库改名 `win-termux` → **`super-termux`**（已经支持三个系统，
名字里的 `win-` 不再合适）。关于页的仓库链接、生成的 `termux.ini` 头部注释、
README 标题同步更新；`history.md` 和描述历史 bug 的代码注释**保持原样**，
不改写过去的记录。

这一版还补上了 Windows 侧缺失的运行时测试。CI 的 `windows` 作业以前只有
「构建 + 静态检查」，一个运行时测试都没有：

- **主机侧回归**：`make unittest`、`make verify-loader`、`verify_all.py`。这些用
  `tests/stub/windows.h` 替身，不需要真终端。为此把两个只在 Windows 上出现的
  坑收进了 `tests/hbuild.py` 和 Makefile 的 `HOST_EXE_EXT`：MinGW 会给【无扩展名】
  的 `-o` 自动补 `.exe`（`-o foo` 产出 `foo.exe`，脚本随后找 `foo` 就挂），
  以及 MinGW 不支持 `-fsanitize`（现在显式降级并打 `[SKIP-SANITIZER]`，不静默）。
- **真 ConPTY 冒烟测试**：新增 `tests/conpty_drive.py`（`CreatePseudoConsole` +
  `PROC_THREAD_ATTRIBUTE_PSEUDOCONSOLE`，接口与 `tests/drive.py` 对齐，读帧的
  方法直接复用）和 `tests/win_smoke.py`。验的是**运行期**证据：关于页/帮助页的
  平台文案（bug #5/#9 以前只做到 `gcc -E` 预处理级）、分屏真的多出窗格、
  resize 之后历史还在 —— 最后一项正是最初那个「resize 丢历史」bug 的老家，
  此前从没有过 Windows 上的自动化复现手段。

**v2.0.1** —— 修正「单文件 C」这个措辞。关于页副标题原来写「基于 …… 的高性能单文件 C
终端复用多标签环境」，「单文件 C」很容易被读成「源码只有一个 C 文件」，而实际是
20 个 `.c` + 18 个 `.h`、约 1.9 万行。真正成立的是**构建产物**是单个可执行文件
（Linux 只依赖 libc，Windows 只链 `user32`/`shell32`）。现在副标题拆成两行：
「基于 …… 的高性能终端复用多标签环境」+「纯 C 实现，单文件可执行」。
GitHub 仓库的 About 与 Topics 同步改为跨平台。

**v2.0.0** —— 正式支持 Windows / Linux / macOS 三个系统。引擎代码三系统共用，
平台相关部分抽到平台层；CI 在三个系统上各跑一遍完整检查并发布二进制。
移植过程中修掉的 7 个真 bug（剪贴板死路、配置路径两层错、宽字符多字搜索、
`plat_write_fd` EAGAIN 丢输入 41%、关于页写死 Windows 文案、命令行不认引号、
启动目录不展开且静默失败）都有验红的回归钉住。

更早的版本详见 [history.md](history.md)。

## 开源协议

本项目基于 [MIT 许可证](LICENSE) 开源。
